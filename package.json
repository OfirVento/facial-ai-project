{
  "name": "facial-ai-project",
  "version": "2.0.0",
  "description": "AI Facial Design Platform — FLAME mesh + DECA reconstruction + clinical consultation",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3334 --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node src/server.js"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
